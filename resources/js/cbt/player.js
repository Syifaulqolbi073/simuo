import './timer';
import './autosave';
import './navigator';

document.addEventListener('DOMContentLoaded', () => {

    const player = document.getElementById('cbt-player');

    if (!player) {
        return;
    }

    console.log('CBT Player Loaded');

});